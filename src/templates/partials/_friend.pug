.circle--fluid
  .circle--fluid--cell.circle--fluid--primary
    a.app--tweet--author
      .app--avatar(style='background-image: url(' + friendObj.profile_image_url + ')')
        img(src=friendObj.profile_image_url)
      h4 #{friendObj.name}
      p @#{friendObj.screen_name}
  .circle--fluid--cell
    a.button.button-text Unfollow
